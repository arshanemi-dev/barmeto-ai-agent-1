import './globals.css';
export const metadata = { title: 'Barmeto — Your automation workspace', description: 'Thoughtful desktop automation. Workflows, schedules, and applications in one place.' };
export default function Layout({ children }) { return <html lang="en"><body>{children}</body></html>; }
